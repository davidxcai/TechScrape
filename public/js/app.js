$(function() {

    // Save function
    $(document).on('click', '.save', function() {
        let id = $(this).attr('id');
        console.log('save')
        console.log('id: ' +id);
    });

    // Delete function

    // Add Note function
});